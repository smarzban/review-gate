{
  "name": "review-gate",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "A multi-model code-review GATE: an agent orchestrates holistic reviews; a thin deterministic spine owns the verdict + trust boundary.",
  "bin": { "review-gate": "dist/cli.js" },
  "scripts": {
    "cli": "tsx src/cli.ts",
    "build": "tsc -p tsconfig.build.json",
    "build:check": "npm run build && git diff --exit-code -- dist/",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.0"
  }
}
