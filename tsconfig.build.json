{
  "//": "Build config: emits the spine to dist/ as plain Node ESM (run with `node`, zero runtime deps)",
  "//2": "so the plugin needs no install-time build. tsconfig.json stays noEmit for typecheck (src+tests).",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "outDir": "dist",
    "declaration": false,
    "sourceMap": false
  },
  "include": ["src"]
}
